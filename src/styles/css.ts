import { css } from 'emotion'

export const spanStyleCss = css`
  font-weight: normal;
  position: absolute;
  transform: rotate(90deg);
  font-size: 15px;
  color: #616161;
  margin: -46px 30px 26px 84px;
  @media screen and (max-width: 520px) {
    font-size: 12px;
    margin: -30px 30px 26px 52px;
  }
`
